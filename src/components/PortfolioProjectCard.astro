---
interface Props {
  project: {
    id: string;
    name: string;
    description: string;
    sector: string;
    sectorColor: string;
    type: string;
    url: string;
    gradient: string;
    image: string;
    features: string[];
    stack: string[];
    highlights: { label: string; value: string }[];
    wip: boolean;
    multilingual: boolean;
    sample: boolean;
  };
  index: number;
}

const { project, index } = Astro.props;
---

<article class="group overflow-hidden rounded-2xl border border-[var(--color-border)] bg-white shadow-sm transition-all hover:shadow-xl">
  <div class={`grid ${index % 2 === 0 ? 'lg:grid-cols-[1.1fr_1fr]' : 'lg:grid-cols-[1fr_1.1fr]'}`}>

    <!-- Screenshot area -->
    <div
      class={`relative overflow-hidden ${index % 2 !== 0 ? 'lg:order-2' : ''} flex flex-col items-center justify-center p-6 lg:p-8`}
      style={`background: ${project.gradient};`}
    >
      <div class="relative w-full rounded-lg overflow-hidden shadow-2xl ring-1 ring-black/10">
        <img
          src={project.image}
          alt={`Screenshot of ${project.name}`}
          class="w-full h-auto block"
          loading="lazy"
          width="600"
          height="400"
        />
      </div>
      <div class="flex flex-wrap justify-center gap-2 mt-5">
        {project.wip ? (
          <span class="rounded-full bg-amber-500/30 px-3 py-1 text-[11px] font-medium text-amber-200 backdrop-blur-sm border border-amber-400/20">
            In development
          </span>
        ) : project.highlights.map((h) => (
          <span class="rounded-full bg-black/25 px-3 py-1 text-[11px] font-medium text-white backdrop-blur-sm">
            {h.label}: <span class="font-bold">{h.value}</span>
          </span>
        ))}
      </div>
    </div>

    <!-- Content side -->
    <div class={`flex flex-col justify-center p-8 lg:p-10 ${index % 2 !== 0 ? 'lg:order-1' : ''}`}>
      <div class="mb-4 flex flex-wrap items-center gap-2">
        <span class={`inline-block rounded-full border px-3 py-1 text-xs font-semibold ${project.sectorColor}`}>
          {project.sector}
        </span>
        <span class="text-xs text-[var(--color-text-muted)]">{project.type}</span>
        {project.wip && (
          <span class="inline-block rounded-full border border-amber-500/30 bg-amber-500/10 px-3 py-1 text-xs font-semibold text-amber-600">
            In development
          </span>
        )}
        {project.multilingual && (
          <span class="inline-block rounded-full border border-teal-400/30 bg-teal-400/10 px-3 py-1 text-xs font-semibold text-teal-600">
            FR + EN
          </span>
        )}
        {project.sample && (
          <span class="inline-block rounded-full border border-neutral-300 bg-neutral-50 px-3 py-1 text-xs font-semibold text-neutral-500">
            Demo
          </span>
        )}
      </div>

      <h2 class="mb-3 text-2xl font-bold text-[var(--color-text)] lg:text-3xl">
        {project.name}
      </h2>

      <p class="mb-6 text-[var(--color-text-muted)] leading-relaxed">
        {project.description}
      </p>

      <div class="mb-6 flex flex-wrap gap-2">
        {project.features.map((f) => (
          <span class="rounded-md bg-[var(--color-bg-alt)] px-2.5 py-1 text-xs font-medium text-[var(--color-text)]">
            {f}
          </span>
        ))}
      </div>

      <div class="mb-8 flex items-center gap-3">
        <span class="text-xs uppercase tracking-wider text-[var(--color-text-light)]">Stack:</span>
        {project.stack.map((s) => (
          <span class="text-xs font-semibold text-[var(--color-accent-text)]">{s}</span>
        ))}
      </div>

      <div class="flex flex-wrap gap-3">
        <a
          href={project.url}
          target={project.sample ? '_self' : '_blank'}
          rel={project.sample ? undefined : 'noopener noreferrer'}
          class="inline-flex items-center gap-2 rounded-xl bg-[var(--color-accent)] px-5 py-2.5 text-sm font-medium text-white transition-all hover:bg-[var(--color-accent-hover)] hover:shadow-lg"
          style="box-shadow: 0 4px 15px rgba(232, 108, 71, 0.2);"
          data-umami-event={`portfolio-visit-${project.id}`}
        >
          View the site
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" /></svg>
        </a>
        {!project.wip && !project.sample && (
          <a
            href={`https://pagespeed.web.dev/analysis?url=${encodeURIComponent(project.url)}`}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 rounded-xl border border-[var(--color-border)] px-5 py-2.5 text-sm font-medium text-[var(--color-text-muted)] transition-all hover:border-[var(--color-accent)] hover:text-[var(--color-accent-text)]"
          >
            Speed score
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" /></svg>
          </a>
        )}
      </div>
    </div>

  </div>
</article>
